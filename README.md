# adweb-eindopdracht

## Hosting
To deploy the app:
- Run `npx firebase login` and login
- Run `npx firebase experiments:enable webframeworks`
- Run `firebase deploy`

To undeploy it (assuming you're still logged in):
- Run `firebase hosting:disable`